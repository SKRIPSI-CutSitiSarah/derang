NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Base URL of the ML service (Flask). Point at the local mock (http://localhost:3000/api/mock-ml)
# until the real service is ready, then swap to the Flask origin — no code changes needed.
ML_SERVICE_URL=http://localhost:3000/api/mock-ml
